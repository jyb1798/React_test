import React from "react";
import styled from "styled-components";
import { Link } from "react-router-dom";

const Nav = () => {
  return (
    <Header>
      <Inner>
        <Link to="/"></Link>
      </Inner>
    </Header>
  );
};

export default Nav;
